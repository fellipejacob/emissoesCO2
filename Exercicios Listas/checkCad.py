user = str (input("Digite o usuário:"))
    
    while len(user) < 3:
        user = str (input("User inválido, digite outro:"))

idade = int (input("Digite a idade:"))
    if idade == int:
        while idade > 150 or idade < 0:
        idade = int (input("Idade inválida, digite outra:"))
    else:
        idade = int(input("Digite a idade em números:")


#verificar o tipo na entrada